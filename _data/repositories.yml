github_users:
  - Yanko-7

repo_description_lines_max: 2

# Open-source projects I work on / contribute to.
# Each entry must be an exact "owner/repo" slug — cards are built from the GitHub API,
# so a wrong slug renders a broken card. Add or remove freely.
github_repos:
  - sgl-project/sglang-jax
  - sgl-project/sglang
  - apache/doris
  - xmake-io/xmake-repo
  # Add more of your own repos or contributions below, for example:
  # - Yanko-7/your-project
  # - owner/kdtree-repo   # replace with the exact KDTree repo slug from your résumé
